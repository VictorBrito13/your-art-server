const fs = require('fs')
const Path = require('path')

const controllers = {
    //*New publication
    new_publication: (req, res) => {
        try{
            
        }catch(err){
            console.error(err)
        }
    }
}

module.exports = controllers